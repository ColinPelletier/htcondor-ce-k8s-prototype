###############################################################################
# TOKEN (IDTOKEN) identity → local Unix account mappings
#
# The token identity is set at creation time via condor_token_create -identity.
# Add one line per identity that is allowed to submit jobs.
###############################################################################
 
# Local test submitter — token created in docker-entrypoint-ce.sh
TOKEN /^testce@users\.htcondor\.org$/ testce